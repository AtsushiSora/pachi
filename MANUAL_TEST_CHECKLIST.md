# 実機確認チェックリスト

ICHIGEKI 一撃スロパチを公開前にスマホ実機で確認するためのチェックリストです。
自動チェックで拾えない操作感、表示崩れ、外部サービス連携を確認します。

## 確認対象

- 公開URL: https://ichigekipachi.netlify.app/
- iPhone Safari
- Android Chrome
- PC Chrome

AdSense審査中は広告枠がプレースホルダー表示でも合格にします。
広告ユニットIDを設定した後は、同じ項目を再確認します。

## 事前確認

- `npm run release:check` が通る
- NetlifyのProduction deployが完了している
- Supabase SQL Editorで `supabase-ranking.sql` が適用済み
- `ads.txt` と `app-ads.txt` が公開URLで開ける
- `privacy.html`、`disclaimer.html`、`safety.html`、`about.html`、`contact.html` が開ける

## トップ

- トップが表示される
- 全国チャレンジが `ranking.html` に移動する
- 実戦シミュレーターが `app.html` に移動する
- スペックシミュレーターが `sim.html` に移動する
- 使い方が `howto.html` に移動する
- スマホに追加の説明が読める
- 画面下固定の広告枠が本文を隠しすぎない

## 全国ランキング

- TOP10 / TOP30を切り替えられる
- 一撃 / 連チャン / ハマりを切り替えられる
- スマホ表示で表彰台が上から1位、2位、3位の順に見える
- ランキング登録済みデータの玉数、連チャン数、初当たり回転数が崩れない
- Supabaseが一時的に読めない時もローカルランキング表示に落ちる

## 全国チャレンジ

- 「チャレンジする」から `challenge.html?start=1` に移動する
- `?start=1` で自動的に変動が始まる
- 突破成功時に連チャンが進む
- 突破失敗時に実行中のまま止まらない
- 完了後に結果登録モーダルが出る
- ニックネームを入れてランキング登録できる
- ニックネーム欄に個人情報を入れない案内が表示される
- メールアドレス風、電話番号風、URL風のニックネームが登録前に弾かれる
- 結果シェアでクリップボードコピーまたは共有UIが動く
- 差玉と使用玉の計算が不自然な結果は登録されない
- ランキング登録後の広告枠が表示される

## スペックシミュレーター

- 「スタート」で試算が開始する
- 大当たり確率、突破率、継続率、250玉あたり回転数のスライダーが動く
- 出玉は100玉単位で補正される
- ラウンドRは1単位で補正される
- 試算結果の総出玉、投資、差玉が更新される
- 「もう一回」を5回使うと広告枠が出る
- 戻るボタンが「スタート」の下にあり、トップへ戻れる

## 実戦シミュレーター

- 高速モードが初期状態でOFF
- STARTで変動が始まる
- STOPで停止する
- スタート連打で体力だけが先に減り続けない
- 変動開始時に体力表示が減る
- 大当たり時に出玉が持ち玉へ加算される
- 初当たり後は投資ではなく持ち玉から消費される
- 広告を見る操作で体力が全回復する
- 投資は円表示として読める

## PWA / オフライン

- iPhone Safariでホーム画面に追加できる
- Android Chromeでアプリをインストール、またはホーム画面に追加できる
- ホーム画面から起動した時にトップが開く
- オフライン時に `offline.html` が表示される
- アイコンがぼやけない

## 合否ログ

| 日付 | 端末 | ブラウザ | 結果 | メモ |
| --- | --- | --- | --- | --- |
|  | iPhone | Safari | 未確認 |  |
|  | Android | Chrome | 未確認 |  |
|  | PC | Chrome | 未確認 |  |
